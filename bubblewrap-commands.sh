# Instalar Bubblewrap CLI (necesitas tener Node.js y Android SDK instalados)
npm install -g @bubblewrap/cli

# Inicializar el proyecto TWA
bubblewrap init --manifest=https://tu-sitio-web.com/manifest.json

# Construir el APK
bubblewrap build

